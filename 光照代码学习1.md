```glsl
Shader "lit/Phong"
{
	// ==========================================================
	// 1. 属性面板 (Properties) 
	// 这里是你能在 Unity 材质球面板上看到的“调节按钮”
	// ==========================================================
	Properties
	{
		_MainTex ("Texture", 2D) = "white" {} // 基础颜色贴图：物体的表皮长什么样
		_NormalMap("NormalMap",2D) = "bump"{} // 法线贴图：记录表面的凹凸细节（蓝紫色的图）
		_NormalIntensity("Normal Intensity",Range(0.0,5.0)) = 1.0 // 凹凸强度：值越大，看着越崎岖
		_AOMap("AO Map",2D) = "white"{} // 环境光遮蔽贴图：记录模型缝隙和犄角旮旯的阴影
		_SpecMask("Spec Mask",2D) = "white"{} // 高光遮罩：黑白图，白色的地方反光，黑色的地方不反光
		_Shininess("Shininess",Range(0.01,100)) = 1.0 // 高光集中度：值越大，物体看起来越光滑，光斑越小越亮
		_SpecIntensity("SpecIntensity",Range(0.01,5)) = 1.0 // 高光亮度：控制反光有多刺眼
		_ParallaxMap("ParallaxMap",2D) = "black"{} // 视差贴图：比法线贴图更猛，能提供“深度感”的黑白图
		_Parallax("_Parallax",float) = 2 // 视差深度：控制“凹陷”下去有多深
	}

	SubShader
	{
		// 告诉 Unity 这是一个不透明的物体
		Tags { "RenderType"="Opaque" }
		LOD 100

		// ==========================================================
		// 第一个 Pass：ForwardBase (前向渲染基础通道)
		// 它的任务是计算：主光源（如太阳光）、环境光、物体本身的颜色、以及阴影
		// ==========================================================
		Pass
		{
			Tags{"LightMode" = "ForwardBase"} 
			CGPROGRAM
			#pragma vertex vert   // 声明：顶点着色器的名字叫 vert
			#pragma fragment frag // 声明：片段(像素)着色器的名字叫 frag
			#pragma multi_compile_fwdbase // 开启前向渲染的基础功能（比如光照衰减、阴影）
			#include "UnityCG.cginc" // 引入 Unity 官方写好的工具箱
			#include "AutoLight.cginc" // 引入处理光照和阴影的工具箱
			
			// 【快递员 1号】appdata：从 3D 模型里读取原始数据
			struct appdata
			{
				float4 vertex : POSITION;  // 顶点在模型自身的局部坐标
				float2 texcoord : TEXCOORD0; // UV 贴图坐标（相当于贴纸在模型上的位置）
				float3 normal  : NORMAL;   // 顶点法线（垂直于表面的方向）
				float4 tangent : TANGENT;  // 顶点切线（平行于表面的方向）
			};

			// 【快递员 2号】v2f：顶点着色器处理完后，打包发给片段（像素）着色器的数据
			struct v2f
			{
				float4 pos : SV_POSITION; // 顶点在屏幕上的最终位置
				float2 uv : TEXCOORD0;    // 传递 UV 坐标
				float3 normal_dir : TEXCOORD1;  // 世界空间下的法线方向
				float3 pos_world : TEXCOORD2;   // 顶点在世界空间下的真实坐标
				float3 tangent_dir : TEXCOORD3; // 世界空间下的切线方向
				float3 binormal_dir : TEXCOORD4;// 世界空间下的副切线方向
				SHADOW_COORDS(5) // 分配一个空间用来接收阴影数据
			};

			// 把上面 Properties 里定义的变量，在代码里声明一遍，才能真正使用
			sampler2D _MainTex;
			float4 _MainTex_ST;
			float4 _LightColor0; // 主光源的颜色（Unity自动传入）
			float _Shininess;
			float4 _AmbientColor;
			float _SpecIntensity;
			sampler2D _AOMap;
			sampler2D _SpecMask;
			sampler2D _NormalMap;
			float _NormalIntensity;
			sampler2D _ParallaxMap;
			float _Parallax;
			
			// 【渲染技术：ACES 电影级色调映射】
			// 作用：物理世界的光极亮，但显示器最高亮度有限。
			// 这个函数把刺眼的、超过 1.0 的超亮颜色，平滑地压缩到 0~1 之间，防止画面“过曝”，让画面有电影感。
			float3 ACESFilm(float3 x)
			{
				float a = 2.51f;
				float b = 0.03f;
				float c = 2.43f;
				float d = 0.59f;
				float e = 0.14f;
				return saturate((x*(a*x + b)) / (x*(c*x + d) + e));
			};

			// ==========================================================
			// 顶点着色器 (Vertex Shader)
			// 作用：算几何题。把模型的点，摆到 3D 世界里，再投影到你的 2D 屏幕上。
			// ==========================================================
			v2f vert (appdata v)
			{
				v2f o;
				// 将顶点从模型局部空间 -> 转换到 -> 屏幕裁剪空间
				o.pos = UnityObjectToClipPos(v.vertex);
				// 处理贴图的缩放和偏移（Tiling 和 Offset）
				o.uv = TRANSFORM_TEX(v.texcoord, _MainTex);
				
				// 构建 TBN 矩阵（法线 Normal、切线 Tangent、副切线 Binormal）
				// 相当于在模型表面建立了一个微型的“上、前、右”指南针坐标系。
				o.normal_dir = normalize(mul(float4(v.normal, 0.0), unity_WorldToObject).xyz);
				o.tangent_dir = normalize(mul(unity_ObjectToWorld, float4(v.tangent.xyz, 0.0)).xyz);
				o.binormal_dir = normalize(cross(o.normal_dir,o.tangent_dir)) * v.tangent.w; // 叉乘算出第三条边
				
				// 记录顶点在 3D 世界里的实际位置
				o.pos_world = mul(unity_ObjectToWorld, v.vertex).xyz;
				
				TRANSFER_SHADOW(o) // Unity 的宏：准备计算该点的阴影
				return o;
			}
			
			// ==========================================================
			// 片段着色器 (Fragment Shader) —— 核心光照魔法在这里！
			// 作用：决定屏幕上每一个像素，到底该显示什么颜色。
			// ==========================================================
			half4 frag (v2f i) : SV_Target
			{		
				half shadow = SHADOW_ATTENUATION(i); // 获取阴影值（0表示纯黑阴影，1表示没阴影）

				// 获取视线方向（摄像机位置 减去 像素在世界的位置）
				half3 view_dir = normalize(_WorldSpaceCameraPos.xyz - i.pos_world);
				
				// 拿到顶点着色器传过来的 TBN 坐标系指南针
				half3 normal_dir = normalize(i.normal_dir);
				half3 tangent_dir = normalize(i.tangent_dir);
				half3 binormal_dir = normalize(i.binormal_dir);
				float3x3 TBN = float3x3(tangent_dir, binormal_dir, normal_dir);
				
				// 把视线方向转换到 TBN 空间（模型表面的微观世界）
				half3 view_tangentspace = normalize(mul(TBN, view_dir));
				half2 uv_parallax = i.uv;

				// 【渲染技术：视差贴图 Parallax Mapping】
				// 粗暴但有效的“步进视差”计算。
				// 循环 10 次，每次去读取一下深度图(_ParallaxMap)，然后根据你观察的视线角度，
				// 偷偷偏移贴图的 UV 坐标。这欺骗了大脑，让你觉得贴图里的坑是真实凹下去的。
				for (int j = 0; j < 10; j++) // 注意这里原代码用了 int i, 为了避免与传入参数 i 冲突，习惯上改用 j
				{
					half height = tex2D(_ParallaxMap, uv_parallax);
					// 核心视差偏移公式：根据高度和视角偏移 UV
					uv_parallax = uv_parallax - (0.5 - height) * view_tangentspace.xy * _Parallax * 0.01f;
				}

				// 用偏移后的新 UV 去读取各种贴图，达到立体效果
				half4 base_color = tex2D(_MainTex, uv_parallax);
				
				// 【渲染技术：伽马转线性空间 (Gamma to Linear)】
				// 图片文件一般是 Gamma 空间的，而光照物理计算必须在 Linear（线性）空间才真实。
				// pow(x, 2.2) 就是把颜色脱去伪装，还原成物理正确的颜色。
				base_color = pow(base_color, 2.2); 
				
				half4 ao_color = tex2D(_AOMap, uv_parallax);      // 读取环境光遮蔽图
				half4 spec_mask = tex2D(_SpecMask, uv_parallax);  // 读取高光遮罩图
				
				// 【渲染技术：法线贴图 Normal Mapping】
				half4 normalmap = tex2D(_NormalMap, uv_parallax); // 读取法线贴图
				half3 normal_data = UnpackNormal(normalmap);      // 解包：把贴图的颜色值转成真正的方向向量
				normal_data.xy = normal_data.xy * _NormalIntensity; // 调整凹凸的强度
				
				// 最后一步：把法线从贴图空间，用 TBN 矩阵扭转到真实的世界空间！
				// 现在的 normal_dir 就是这个像素点真正朝向外面的方向，表面崎岖不平全靠它！
				normal_dir = normalize(mul(normal_data.xyz, TBN));

				// ------------------------- 【Blinn-Phong 光照计算开始】 -------------------------
				
				half3 light_dir = normalize(_WorldSpaceLightPos0.xyz); // 获取主光源（太阳光）的方向

				// 1. 【漫反射 Diffuse】：物体最基础的光影
				// 原理：光线方向 (light_dir) 和 表面法线 (normal_dir) 越平行，光越亮。
				// dot (点乘) 用于算两个箭头的夹角。夹角为0度时 dot 是 1，90度以上是 0。
				half diff_term = min(shadow, max(0.0, dot(normal_dir, light_dir))); // 结合阴影计算亮度
				half3 diffuse_color = diff_term * _LightColor0.xyz * base_color.xyz; // 最终漫反射 = 亮度 * 光颜色 * 物体本色
				
				// 2. 【高光 Specular】：物体表面的反光亮斑
				// 原理：算出 光线和视线 中间的那个方向（半角向量 half_dir）。
				// 如果半角向量跟物体表面的法线重合，说明这束光刚好反射进你的眼睛！
				half3 half_dir = normalize(light_dir + view_dir); 
				half NdotH = dot(normal_dir, half_dir);
				// pow(..., _Shininess)：求 NdotH 的 _Shininess 次方。
				// 这会让稍微偏离的反射光瞬间变暗，从而把高光“聚拢”成一个锐利的白点。
				half3 spec_color = pow(max(0.0, NdotH), _Shininess) 
					 * diff_term * _LightColor0.xyz * _SpecIntensity * spec_mask.rgb; // 乘上高光遮罩，黑色的地方就不反光了

				// 3. 【环境光 Ambient】：
				// 没有被光直接照到的地方也不能是纯黑的，所以铺一层环境底色。
				half3 ambient_color = UNITY_LIGHTMODEL_AMBIENT.rgb * base_color.xyz;
				
				// ------------------------- 【光照计算结束】 -------------------------

				// 【渲染技术：环境光遮蔽 AO】
				// 将 漫反射 + 高光 + 环境光 全部加起来。
				// 然后乘以 ao_color（缝隙处是黑色的，值为0）。这样模型的缝隙、角落就会变暗，立体感增强。
				half3 final_color = (diffuse_color + spec_color + ambient_color) * ao_color;
				
				// 【色调映射】：通过 ACES 函数把可能超过 1.0 的超亮像素压回正常范围，防过曝。
				half3 tone_color = ACESFilm(final_color);
				
				// 【渲染技术：线性转伽马空间 (Linear to Gamma)】
				// 算完了物理光照，在把图像输出给显示器前，还要把它变回显示器喜欢的 Gamma 格式。
				// pow(x, 1.0/2.2) 就是最后这步转换。
				tone_color = pow(tone_color, 1.0 / 2.2);
				
				// 输出最终的颜色！半透明度(Alpha)设为 1.0 (完全不透明)
				return half4(tone_color, 1.0);
			}
			ENDCG
		}
		
		// ==========================================================
		// 第二个 Pass：ForwardAdd (前向渲染附加通道)
		// 它的任务是计算：场景里的“额外光源”（比如角色的火把、路灯、点光源）
		// 这个通道会被调用多次（如果有多个灯的话）。
		// ==========================================================
		Pass
		{
			Tags{"LightMode" = "ForwardAdd"}
			// 【关键设置】：Blend One One 意味着“加法混合”。
			// 把这个灯光算出来的颜色，直接“叠加”到上一个 Pass 画好的图像上，而不是覆盖它。
			Blend One One 
			
			CGPROGRAM
			#pragma vertex vert
			#pragma fragment frag
			#pragma multi_compile_fwdadd // 开启附加灯光的宏支持
			#include "UnityCG.cginc"
			#include "AutoLight.cginc"

			// 以下的数据结构和刚才的 ForwardBase 几乎一模一样
			struct appdata
			{
				float4 vertex : POSITION;
				float2 texcoord : TEXCOORD0;
				float3 normal  : NORMAL;
				float4 tangent : TANGENT;
			};

			struct v2f
			{
				float4 pos : SV_POSITION;
				float2 uv : TEXCOORD0;
				float3 normal_dir : TEXCOORD1;
				float3 pos_world : TEXCOORD2;
				float3 tangent_dir : TEXCOORD3;
				float3 binormal_dir : TEXCOORD4;
				LIGHTING_COORDS(5, 6) // 处理点光源/聚光灯光照衰减的坐标
			};

			sampler2D _MainTex;
			float4 _MainTex_ST;
			float4 _LightColor0; // 这里的 _LightColor0 变成了“当前处理的这个点光源”的颜色
			float _Shininess;
			float4 _AmbientColor;
			float _SpecIntensity;
			sampler2D _AOMap;
			sampler2D _SpecMask;
			sampler2D _NormalMap;
			float _NormalIntensity;
			sampler2D _ParallaxMap;
			float _Parallax;

			// ACES 色调映射
			float3 ACESFilm(float3 x)
			{
				float a = 2.51f;
				float b = 0.03f;
				float c = 2.43f;
				float d = 0.59f;
				float e = 0.14f;
				return saturate((x*(a*x + b)) / (x*(c*x + d) + e));
			};

			v2f vert(appdata v)
			{
				v2f o;
				o.pos = UnityObjectToClipPos(v.vertex);
				o.uv = TRANSFORM_TEX(v.texcoord, _MainTex);
				o.normal_dir = normalize(mul(float4(v.normal, 0.0), unity_WorldToObject).xyz);
				o.tangent_dir = normalize(mul(unity_ObjectToWorld, float4(v.tangent.xyz, 0.0)).xyz);
				o.binormal_dir = normalize(cross(o.normal_dir,o.tangent_dir)) * v.tangent.w;
				o.pos_world = mul(unity_ObjectToWorld, v.vertex).xyz;
				TRANSFER_VERTEX_TO_FRAGMENT(o);
				return o;
			}

			half4 frag(v2f i) : SV_Target
			{
				// 获取点光源的衰减值（灯光离得越远越暗）
				half atten = LIGHT_ATTENUATION(i);

				// 同上：计算视线和视差效果
				half3 view_dir = normalize(_WorldSpaceCameraPos.xyz - i.pos_world);
				half3 normal_dir = normalize(i.normal_dir);
				half3 tangent_dir = normalize(i.tangent_dir);
				half3 binormal_dir = normalize(i.binormal_dir);
				float3x3 TBN = float3x3(tangent_dir, binormal_dir, normal_dir);
				half3 view_tangentspace = normalize(mul(TBN, view_dir));
				half2 uv_parallax = i.uv;

				for (int j = 0; j < 10; j++)
				{
					half height = tex2D(_ParallaxMap, uv_parallax);
					uv_parallax = uv_parallax - (0.5 - height) * view_tangentspace.xy * _Parallax * 0.01f;
				}

				// 同上：读取各种贴图
				half4 base_color = tex2D(_MainTex, uv_parallax);
				// 【注意】这里省去了 base_color 的 pow(2.2) 转换，严格来说，如果是 PBR 流程这里也该转一下。
				// 但作为附加光源，有时候为了性能或特定美术效果会省略。
				half4 ao_color = tex2D(_AOMap, uv_parallax);
				half4 spec_mask = tex2D(_SpecMask, uv_parallax);
				half4 normalmap = tex2D(_NormalMap, uv_parallax);
				half3 normal_data = UnpackNormal(normalmap);
				normal_data.xy = normal_data.xy * _NormalIntensity;
				normal_dir = normalize(mul(normal_data.xyz, TBN));

				// 【核心区别：点光源方向】
				// 太阳光(平行光)方向是固定的；但点光源(火把)的方向，是灯的位置 减去 像素的位置！
				half3 light_dir_point = normalize(_WorldSpaceLightPos0.xyz - i.pos_world);
				half3 light_dir = normalize(_WorldSpaceLightPos0.xyz); // 平行光方向
				// lerp 是插值函数。Unity用 _WorldSpaceLightPos0.w 来标记灯光类型，巧妙地混合了平行光和点光源的算法。
				light_dir = lerp(light_dir, light_dir_point, _WorldSpaceLightPos0.w);
				
				// 点光源的漫反射：结合了光源距离衰减 (atten)
				half diff_term = min(atten, max(0.0, dot(normal_dir, light_dir)));
				half3 diffuse_color = diff_term * _LightColor0.xyz * base_color.xyz;

				// 点光源的高光
				half3 half_dir = normalize(light_dir + view_dir);
				half NdotH = dot(normal_dir, half_dir);
				half3 spec_color = pow(max(0.0, NdotH),_Shininess)
					 * diff_term * _LightColor0.xyz * _SpecIntensity * spec_mask.rgb;

				// 附加通道不需要加环境光了（上一个通道加过一次了，再加就会导致重叠发白）
				// 也不用做 ACES 和 Gamma 转换，直接把这个灯的增量颜色输出去，交给底层去做 Blend One One 叠加。
				half3 final_color = (diffuse_color + spec_color) * ao_color;
				return half4(final_color, 1.0);
			}
			ENDCG
		}
	}
	// 如果玩家的显卡太烂，跑不动上面的代码，就退回到最基础的自带 "Diffuse" 材质。
	Fallback "Diffuse"
}
```