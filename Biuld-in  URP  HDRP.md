

## 1. 核心概念

渲染管线决定了 Unity 如何将场景中的光照、摄像机、材质等数据，计算并最终绘制成屏幕上的像素。

**Unity 的方向转变：**
- **过去：** Built-in（内置管线），闭源的黑盒架构。
- **现在与未来：** SRP（Scriptable Render Pipeline，可编程渲染管线），开源且基于 C#，分为 URP 和 HDRP。

---

## 2. 三大渲染管线详细对比表

| 对比维度          | Built-in (内置管线)          | URP (通用渲染管线)                                     | HDRP (高清渲染管线)                               |
| :------------ | :----------------------- | :----------------------------------------------- | :------------------------------------------ |
| **全称**        | Built-in Render Pipeline | Universal Render Pipeline (原名 LWRP)              | High Definition Render Pipeline             |
| **底层架构**      | C++ 黑盒架构 (极难修改底层)        | SRP，基于 C# (高度模块化，易扩展)                            | SRP，基于 C# (高度模块化，但极度复杂)                     |
| **核心定位**      | 历史遗留，兼容老旧项目              | 性能至上，"Write once, run anywhere"                  | 画质至上，追求极致物理真实感                              |
| **目标平台**      | 所有老平台                    | **全平台** (手机、VR、Switch、PC、主机)                     | **仅高端硬件** (中高配PC、PS5/Xbox Series)           |
| **不支持平台**     | 无                        | 无                                                | **手机移动端、Web、低端设备**                          |
| **核心光照技术**    | 多通道前向渲染 (耗性能) / 延迟渲染     | 单通道前向渲染 (Forward+) / 延迟渲染                        | 基于 Compute Shader 的光照 / 光线追踪 (Ray Tracing)  |
| **Shader 编写** | Cg/HLSL，Standard Shader  | HLSL / **Shader Graph (主力)**                     | HLSL / Shader Graph / 物理精确材质                |
| **美术风格强项**    | 无明显优势                    | **风格化 (Stylized)** / 卡通渲染 (NPR) / 2D             | **极致写实 (Photorealistic)**                   |
| **特色功能/优势**   | 兼容所有老插件                  | **专有 2D Renderer** / Render Features (渲染特性) 极易扩展 | 体积云雾 / SSS(次表面散射) / Clear Coat(透明涂层) / 真实曝光 |
| **性能消耗**      | 中等                       | **极低 (优化极佳)**                                    | 极高                                          |

---

## 3. 各管线深度解析

### Built-in (内置渲染管线)
Unity 最古老的默认管线。官方已经停止为其开发新图形功能，仅做基础维护。
- **优点：** 兼容性无敌，老旧的 Asset Store 插件基本都支持。
- **缺点：** 架构老旧，前向渲染模式下处理多光源性能极差。开发者无法介入渲染底层。
- **结论：** 除非是在维护很多年前的老项目，否则**绝对不要**在新项目中使用。

### URP (通用渲染管线)
Unity 目前的主力推崇管线，90% 以上新项目的首选。
- **架构优势：** 通过 `Render Features`（渲染特性），可以用简单的 C# 脚本在渲染流程中插入自定义效果（如全屏后处理、特定物体的高光轮廓等），而不用改底层代码。
- **性能优势：** 它的单通道前向渲染（Single-pass Forward Rendering）能在一个 Pass 里计算多个光源，大幅减少 Draw Call。
- **2D 专属：** URP 是目前唯一拥有专门 `2D Renderer` 的管线，支持 2D 光照和 2D 阴影。

### HDRP (高清渲染管线)
为了对标虚幻引擎 (Unreal) 顶级画质而生的管线，纯粹的“重火力”。
- **物理精确：** 光照单位使用真实的 Lux（勒克斯）或 Lumen（流明），摄像机设置完全模拟真实单反（ISO、光圈、快门）。
- **高级材质：** 默认支持次表面散射（做真实的皮肤透光）、毛发渲染。
- **门槛极高：** 学习曲线陡峭，对模型面数、贴图分辨率要求极高，且完全无法在手机上运行。

---

## 4. 选型决策树 (Decision Tree)

1. **项目是手机游戏、VR/AR 游戏、Switch 游戏，或者是纯 2D 游戏？**
   - ➡️ **无脑选 URP。**
2. **项目是追求电影级画质的 PC 端游、3A 主机游戏，或者是用来做影视动画、建筑可视化的？**
   - ➡️ **选 HDRP。**
3. **我是个新手，我想做个简单的 3D 游戏练手？**
   - ➡️ **选 URP。** (学习成本低，生态最好，且与目前的 Shader Graph 配合最完美)。

---

## 5. 进阶学习路线规划

作为游戏开发和 Shader 编写的初学者，建议按以下顺序攻克：

1. **放弃 Built-in：** 不要浪费时间研究 Built-in 的 Cg 代码和复杂的 Pass 结构。
2. **死磕 URP：**
   - 学习 URP 的资产配置和项目升级。
   - 学习如何编写一个简单的 `Render Feature`（例如实现一个最基础的屏幕灰度效果）。
3. **掌握 Shader Graph：**
   - 在 URP 环境下，通过节点连线学习基础的图形学数学（点乘、叉乘、UV 流动）。
   - 尝试用 Shader Graph 实现卡通渲染 (Toon Shading) 效果。
4. **进阶 HLSL (可选)：**
   - 当节点连线无法满足需求，或者为了极致的性能优化时，再学习在 URP 下手写 HLSL Shader。