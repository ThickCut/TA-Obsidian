# Blinn-Phong

## 一、光照计算基础 

计算光照的本质，是将不同类型的光照效果经过数学运算后叠加在一起，输出最终的像素颜色。

### 1. 核心方向向量

所有光照计算都依赖于以下四个关键向量（通常在片段着色器中需要被标准化 `normalize`）：

- **$\mathbf{N}$ (Normal，法线向量)**：垂直于物体表面，决定表面的朝向。

- **$\mathbf{L}$ (Light，光照方向)**：从着色点指向光源的方向。

- **$\mathbf{V}$ (View，观察方向)**：从着色点指向摄像机（视点）的方向。

- **$\mathbf{H}$ (Half，半角向量)**：$\mathbf{L}$ 和 $\mathbf{V}$ 的中间向量。用于简化高光计算，公式为 $\frac{\mathbf{L} + \mathbf{V}}{\vert{}\mathbf{L} + \mathbf{V}\vert{}}$。


### 2. 光照分量计算

最终的画面颜色由四个部分叠加而成：$Color_{final} = Ambient + Diffuse + Specular + Emission$

- **环境光 (Ambient)**
    
    - **作用**：模拟光线在环境中的多次弹射，照亮暗部。
    
    - **公式**：$Ambient = LightColor \times AmbientIntensity$
    
- **漫反射 (Diffuse)**
    
    - **作用**：表现物体的固有色和受光面的明暗变化（兰伯特定律）。
    
    - **公式**：$Diffuse = LightColor \times Albedo \times \max(0, \mathbf{N} \cdot \mathbf{L})$
    
- **高光反射 (Specular)**
    
    - **作用**：表现光滑表面上的强烈反光斑。
    
    - **公式**：$Specular = LightColor \times SpecularColor \times \max(0, \mathbf{N} \cdot \mathbf{H})^{Gloss}$
    
    - **参数**：$Gloss$ (光泽度) 决定光斑大小，值越大，光斑越集中、越锐利。
    

## 二、代码书写逻辑 (实践篇)

在手写 Shader（尤其是在 Unity URP 的 HLSL 语境下）时，光照计算的逻辑流程通常遵循以下固定套路，分为三个阶段传递数据：

### 1. 结构体定义阶段 (Structs)

需要定义顶点着色器的输入 (`appdata`) 和输出 (`v2f`，即传递给片段着色器的数据)。

- **输入流**：获取模型空间下的顶点位置 (OS Position) 和法线 (OS Normal)。

- **输出流**：必须准备好**世界空间的法线**和**世界空间的顶点坐标**，因为光照计算通常在世界空间 (World Space) 下进行最为直观。


### 2. 顶点着色器阶段 (Vertex Shader)

在这个阶段完成空间转换，为光照计算准备好“舞台”。

- 将顶点从模型空间转换到裁剪空间（用于屏幕显示）。

- 将法线从模型空间转换到世界空间。

- 将顶点坐标从模型空间转换到世界空间（稍后用于计算 $\mathbf{V}$ 和 $\mathbf{L}$）。


### 3. 片段着色器阶段 (Fragment Shader)

这是光照计算的核心区域，按照以下逻辑顺序书写：

1. **标准化 (Normalize)**：将顶点着色器传过来的世界法线 $\mathbf{N}$ 进行标准化，防止插值导致向量长度改变。

2. **获取光源数据**：在 URP 中，通常通过 `GetMainLight()` 获取主光源的颜色 (`light.color`) 和方向 (`light.direction`，即 $\mathbf{L}$)。

3. **计算观察方向**：通过摄像机世界坐标减去当前像素的世界坐标，得到 $\mathbf{V}$ 并标准化。

4. **计算半角向量**：将 $\mathbf{L}$ 和 $\mathbf{V}$ 相加并标准化，得到 $\mathbf{H}$。
    
5. **分量计算**：
    
    - 利用 `dot(N, L)` 计算漫反射系数，记得用 `max(0, ...)` 截断负数。
        
    - 利用 `dot(N, H)` 计算高光系数，并套用 `pow(..., Gloss)` 计算光泽度。
        
6. **叠加输出**：将 环境光 + (漫反射系数 × 光源颜色 × 贴图颜色) + (高光系数 × 光源颜色 × 高光颜色) 叠加，作为最终的 `half4` 颜色返回。
```hlsl
Shader "Custom/URP_BlinnPhong"
{
    Properties
    {
        _MainColor ("主颜色 (Albedo)", Color) = (1, 1, 1, 1)
        _SpecularColor ("高光颜色 (Specular)", Color) = (1, 1, 1, 1)
        _Gloss ("光泽度 (Gloss)", Range(1, 256)) = 20
        _AmbientColor ("环境光颜色 (Ambient)", Color) = (0.1, 0.1, 0.1, 1)
    }

    SubShader
    {
        // 声明这是 URP 管线下的不透明物体
        Tags { "RenderType"="Opaque" "RenderPipeline"="UniversalPipeline" }

        Pass
        {
            Name "ForwardLit"
            Tags { "LightMode"="UniversalForward" }

            HLSLPROGRAM
            #pragma vertex vert
            #pragma fragment frag

            // 引入 URP 核心库，包含常用的空间转换矩阵和光源计算函数
            #include "Packages/com.unity.render-pipelines.universal/ShaderLibrary/Core.hlsl"
            #include "Packages/com.unity.render-pipelines.universal/ShaderLibrary/Lighting.hlsl"

            // 材质面板参数声明
            CBUFFER_START(UnityPerMaterial)
                half4 _MainColor;
                half4 _SpecularColor;
                float _Gloss;
                half4 _AmbientColor;
            CBUFFER_END

            // ==========================================
            // 阶段 1：结构体定义阶段 (Structs)
            // ==========================================
            
            // appdata: 从模型网格读取的数据
            struct Attributes
            {
                float4 positionOS : POSITION; // 模型空间顶点位置
                float3 normalOS : NORMAL;     // 模型空间法线
            };

            // v2f: 从顶点着色器传递给片段着色器的数据
            struct Varyings
            {
                float4 positionCS : SV_POSITION; // 裁剪空间顶点位置 (必须有)
                float3 positionWS : TEXCOORD0;   // 世界空间顶点位置
                float3 normalWS : TEXCOORD1;     // 世界空间法线
            };

            // ==========================================
            // 阶段 2：顶点着色器阶段 (Vertex Shader)
            // ==========================================
            Varyings vert(Attributes input)
            {
                Varyings output;
                
                // 1. 将顶点从模型空间转换到世界空间
                output.positionWS = TransformObjectToWorld(input.positionOS.xyz);
                
                // 2. 将顶点从世界空间转换到裁剪空间（屏幕显示需要）
                output.positionCS = TransformWorldToHClip(output.positionWS);
                
                // 3. 将法线从模型空间转换到世界空间
                output.normalWS = TransformObjectToWorldNormal(input.normalOS);
                
                return output;
            }

            // ==========================================
            // 阶段 3：片段着色器阶段 (Fragment Shader)
            // ==========================================
            half4 frag(Varyings input) : SV_Target
            {
                // 1. 核心向量准备与标准化
                float3 N = normalize(input.normalWS); // 法线向量 N
                
                // 获取 URP 主光源数据
                Light mainLight = GetMainLight();
                float3 L = normalize(mainLight.direction); // 光照方向 L
                half3 lightColor = mainLight.color;        // 光源颜色
                
                // 计算观察方向 V (摄像机世界位置 - 像素世界位置)
                // URP 提供了快捷函数 GetWorldSpaceNormalizeViewDir
                float3 V = GetWorldSpaceNormalizeViewDir(input.positionWS);
                
                // 计算半角向量 H
                float3 H = normalize(L + V);

                // 2. 分步计算光照分量
                
                // Step A: 环境光 (Ambient)
                // 在简单的自定义 shader 中，我们可以直接用一个颜色替代复杂的环境光采样
                half3 ambient = _AmbientColor.rgb;

                // Step B: 漫反射 (Diffuse)
                // saturate() 是 HLSL 中的函数，等同于笔记里的 max(0, x)，将结果限制在 0-1 之间
                float NdotL = saturate(dot(N, L)); 
                half3 diffuse = lightColor * _MainColor.rgb * NdotL;

                // Step C: 高光反射 (Specular)
                float NdotH = saturate(dot(N, H));
                half3 specular = lightColor * _SpecularColor.rgb * pow(NdotH, _Gloss);

                // 3. 颜色合并 (Combine)
                half3 finalColor = ambient + diffuse + specular;

                // 返回最终颜色，Alpha 通道设为 1
                return half4(finalColor, 1.0);
            }
            ENDHLSL
        }
    }
}
```