---
tags:
  - TA
  - 绑定
  - 蒙皮
  - 入门
  - 前置知识
created: 2026-08-15
related:
  - "[[FEM蒙皮 vs 关节蒙皮-迪士尼与战神]]"
---

# 绑定与蒙皮:前置知识入门(小白向)

> 配套阅读:[[FEM蒙皮 vs 关节蒙皮-迪士尼与战神]]。那篇讲了"迪士尼 FEM 仿真"和"战神关节+RBF"两条路线,这篇把读懂它们需要的**地基**补齐。
>
> 目标读者:想走技术美术/绑定(TA·Rigging)方向、但对骨骼、蒙皮、数学、物理还比较陌生的人。

---

## 0. 先建立全局认知:一条管线

角色动画在引擎里跑起来,大致是这条链:

```
建模(Modeling) ─► 绑定(Rigging) ─► 蒙皮(Skinning) ─► 动画(Animation) ─► 引擎播放
    网格            骨架+控制器        骨架驱动网格        骨骼动起来          实时渲染
```

- **绑定 Rigging**:给角色搭一套"骨架"和控制手柄,让动画师能方便地摆姿势。
- **蒙皮 Skinning**:规定"骨架动的时候,网格上的每个顶点跟着动多少"。
- 迪士尼 FEM 和战神 RBF,本质都是在**改良蒙皮这一步**——让它更接近真实肌肉,而不是只在绑定上做文章。

你只需要先记住:**骨架是"骨骼+权重",蒙皮是"骨架怎么拽着网格动"。**

---

## 1. 绑定(Rigging)基础概念

### 1.1 骨架 / 关节 / 骨骼

- **关节(Joint)**:骨架上的节点,有位置和朝向,通常组织成一棵**层级树**(父关节带动子关节,像手臂带动手)。
- **骨骼(Bone)**:连接父关节和子关节的那一段,常用来定义"朝向"。
- **骨架(Skeleton)**:整棵关节树。

```
        spine(脊椎)
          │
    ┌─────┴─────┐
 shoulder      ...
    │
 elbow(肘)
    │
 wrist(腕)
```

> 约定俗成:**"骨骼动画"里的"骨骼",在引擎里其实就是一堆带层级关系的变换矩阵**。别被"骨头"这个词骗了,它不是医学骨头,是一套坐标系。

### 1.2 关键:每个关节是一个"坐标系变换"

每个关节存一个 **局部变换(Local Transform,相对父关节)**,往上累乘得到**世界变换(World Transform)**。

```
world_joint = world_parent × local_joint
```

这就是层级动画的全部本质:**改一个 local,整条子树跟着动。**

### 1.3 绑定里还常有的东西

| 名称 | 作用 |
|------|------|
| **控制器(Controller/NURBS Curve)** | 动画师实际操作的"手柄",比直接抓骨骼好用 |
| **约束(Constraint)** | 让一个物体跟随/朝向/贴附另一个(如 aim、parent、point) |
| **驱动(Driver/Set Driven Key)** | 用一个属性(如肘弯曲角度)自动驱动别的属性 |
| **IK / FK** | IK=手拉脚,身体自动跟;FK=一节节手动转。角色通常两套切换 |
| **形变器(Deformer)** | 直接改网格形状:BlendShape、Lattice、Pose Deformer 等 |

> **Pose Deformer(Maya)/ Corrective Shape Keys(Blender)就是 RBF/Pose Space Deformation 的商用实现**,战神那套 RBF 辅助关节的思想源头。

---

## 2. 蒙皮核心:线性混合蒙皮(LBS)

### 2.1 核心公式

每个顶点被**多个关节**影响,每个关节有个**权重**,顶点位置是这些关节变换结果的加权平均:

$$
\mathbf{v}' = \sum_{j} w_j \cdot (M_j \cdot M_{bind,j}^{-1}) \cdot \mathbf{v}
$$

- $\mathbf{v}$:顶点在**绑定姿势(bind pose)**下的初始位置;
- $M_j$:关节 $j$ 当前的世界矩阵;
- $M_{bind,j}^{-1}$:关节 $j$ 绑定姿势世界矩阵的**逆**,作用是把顶点"转回关节局部空间";
- $w_j$:权重,通常 $\sum w_j = 1$ 且 $w_j \ge 0$。

> **为什么要 $M_{bind}^{-1}$?** 蒙皮时顶点是"跟着关节走的",所以要先把顶点从世界坐标"倒回"到关节坐标系,再"跟着"关节的新世界矩阵出去。这一步叫 **bind inverse**,是理解一切蒙皮的关键。

### 2.2 权重(Weights)怎么来

- 在 DCC(Maya/Blender)里用**刷权重**工具,给每个顶点分配它受哪些关节、各占多少影响;
- 一般一个顶点最多 4 个关节(游戏),所以 vertex shader 里常见 `float4 weights + uint4 joints`(见上篇第五节)。

### 2.3 LBS 的致命问题:糖果纸塌陷

关节强旋转时,两个关节矩阵线性平均,顶点会往**中心收缩**——肘部弯曲时胳膊"变细",这就是"candy-wrapper"塌陷。

```
LBS(会塌)                       想要的(保体积)
   /\        ──旋转──►            ()
  /  \         (细了)             ()
```

- 上一篇讲的**战神"沿纤维方向多插关节"**、**双四元数蒙皮 DQS**、**迪士尼 FEM**,全都是在解决这个"形变不自然/不保体积"的问题。
- 先记住结论:**LBS 是又快又烂的基线,所有高级蒙皮都在它的基础上打补丁。**

---

## 3. 数学前置(按重要性排序)

### 3.1 向量:点积、叉积(必会)

- **点积 Dot**:$\mathbf{a}\cdot\mathbf{b}=\|\mathbf{a}\|\|\mathbf{b}\|\cos\theta$ → 判角度、投影、算"同向还是反向"。
- **叉积 Cross**:$\mathbf{a}\times\mathbf{b}$ → 得到垂直向量,算旋转轴、法线。

### 3.2 矩阵与齐次坐标(必会)

- 三维变换用 **4×4 齐次矩阵**统一表示平移/旋转/缩放:

$$
M = \begin{bmatrix} R & t \\ 0 & 1 \end{bmatrix}
$$

- 变换顺序:先缩放、再旋转、最后平移(列向量从右往左乘)。
- **求逆** = "反向变换",bind inverse 就是求逆。

### 3.3 四元数(Quaternion)(必会)

- 表示旋转:一个标量 + 三个虚部 $\mathbf{q}=(w, x, y, z)$。
- 优点:**不会万向锁**、插值(球面插值 slerp)自然、省存储。
- 旋转向量 $(\theta, \hat{\mathbf{n}})$ → 四元数 $(\cos\frac{\theta}{2}, \hat{\mathbf{n}}\sin\frac{\theta}{2})$。
- 上篇 DQS 用**对偶四元数**(两个四元数组成)同时表示旋转+平移,就是为了平滑保体积地混合刚体变换。

### 3.4 微积分:导数与梯度(FEM 需要)

- **导数 = 变化率**;多元函数的**梯度 $\nabla$** 指向"上升最快方向"。
- FEM 里"能量对顶点位置求导 = 内力",这就是**梯度**。会算梯度、会看 Jacobian(雅可比),FEM 就能读懂一半。

### 3.5 极分解与 SVD(FEM 需要)

- 任何矩阵可分解成 $F = U \Sigma V^T$(SVD)。
- **极分解** $F = RS$:把"形变"拆成"刚体旋转 $R$"+"纯拉伸 $S$"。共旋弹性就靠它把旋转抽走,只对纯形变算能量(见上篇 2.3 的 `polarRotation`)。

---

## 4. 物理/力学前置(读 FEM 必需)

### 4.1 应力与应变

- **应变 Strain**:物体"被拉伸/压缩了多少",无量纲(如拉长 10% → 应变 0.1)。
- **应力 Stress**:单位面积上受到的内力(压强大小的量),单位 Pa。
- 关系:**应力 = 材料参数 × 应变**(胡克定律的最简单形式)。

### 4.2 形变梯度 $\mathbf{F}$

$$
\mathbf{F} = \frac{\partial \mathbf{x}}{\partial \mathbf{X}}
$$

- $\mathbf{X}$ = 参考(未变形)位置,$\mathbf{x}$ = 变形后位置;
- $\mathbf{F}$ 描述"这块材料被怎么拉扯、旋转、剪切"了,**是整个连续介质力学的核心量**。
- $J = \det \mathbf{F}$ 是**体积变化率**:$J=1$ 体积不变,$J>1$ 膨胀(这也是"保体积"在公式里的体现)。

### 4.3 弹性材料模型

- **线性弹性(胡克)**:$\sigma = E\varepsilon$,只在**小形变**下准,大形变会出错。
- **超弹性(Hyperelastic)**:定义**能量函数** $\Psi(\mathbf{F})$,应力 = 能量对 $\mathbf{F}$ 求导。迪士尼用的 Neo-Hookean / 共旋 StVK 都是超弹性,能处理**大形变**。
- 材料常数:**杨氏模量 $E$**(软硬)、**泊松比 $\nu$**(压缩时会不会鼓),换算成拉梅常数 $\mu,\lambda$(上篇 2.2 有公式)。

### 4.4 拉格朗日 vs 欧拉描述

- **拉格朗日**:跟着材料点走(适合固体/肌肉,FEM 默认);
- **欧拉**:固定在空间网格看流体流经(适合水/雪,MPM 用)。
- 记一句:**固体用 FEM(拉格朗日),流体/雪用 MPM(欧拉混合)。**《冰雪奇缘》的雪、Zootopia 的部分仿真就是 MPM。

---

## 5. 数值方法前置(读代码/写代码必需)

### 5.1 时间积分:显式 vs 隐式 vs Verlet

- **显式欧拉**:用当前力直接推位置,简单但**不稳**(步长大了会"爆炸")。
- **隐式欧拉(Backward Euler)**:解"下一帧位置"的方程,稳但贵。**FEM 大形变用隐式**(上篇 2.4)。
- **Verlet 积分**:不单独存速度,用"当前位置 - 上一帧位置"隐含速度,稳、实现简单。**动态骨骼/jiggle 全用它**(上篇 3.5)。

```cpp
// Verlet 一句话核心
vel   = pos - prev;          // 速度隐含在位移里
prev  = pos;
pos  += vel * damping + gravity * dt * dt;   // 惯性 + 重力
```

### 5.2 解线性系统

- 形式 $A\mathbf{x} = \mathbf{b}$,FEM 每步都要解这种巨型稀疏方程;
- 方法从**高斯消元**(小)→ **共轭梯度 CG**(对称正定大)→ **多重网格 Multigrid**(超快,迪士尼用)。
- 你只要知道:**"解线性系统"是 FEM 的发动机,多重网格是它的涡轮增压。**

### 5.3 Newton 迭代

- 非线性方程(弹性就是非线性的)没法一次解,就**线性化 → 解一步 → 再线性化**循环,直到残差够小:

```
repeat:
    r = -f(x)          # 残差(不平衡力)
    if |r| < tol: break
    解 K·dx = r        # 用切线刚度 K(见上篇 2.3)
    x += dx
```

### 5.4 有限元法(FEM)的思想(一句话)

**把连续物体切成许多小块(单元/网格),在每个小块上用简单函数近似,拼起来解全局。** 切得越细越准,但越贵。上篇的"8 节点六面体"就是其中一种块。

---

## 6. 插值:RBF(读战神方案必需)

- **插值 Interpolation**:已知几个点的值,求中间任意点的值(线性插值是最简单的)。
- **径向基函数 RBF**:用"离已知点的距离"做权重的插值,能处理**多维、散乱**的数据点:

$$
f(\mathbf{p}) = \sum_k w_k \, \varphi(\|\mathbf{p} - \mathbf{p}_k\|)
$$

- 训练:解线性系统 $\Phi\,w = d$ 求出 $w$;运行时:几次乘加就出结果。**离线训练、运行时零成本**——这就是它能进实时游戏的原因。
- 它等价于"一个带径向非线性的神经网络",所以理解了 RBF 也就摸到了"数据驱动蒙皮"的门。

---

## 7. 术语表(中英对照,随手查)

| 中文 | 英文 | 一句话 |
|------|------|--------|
| 绑定 | Rigging | 搭骨架+控制器 |
| 蒙皮 | Skinning | 骨架驱动网格 |
| 骨架 | Skeleton | 关节层级树 |
| 关节/骨骼 | Joint / Bone | 一个带变换的坐标系 |
| 权重 | Weight | 关节对顶点影响比例 |
| 绑定姿势 | Bind Pose | 蒙皮时的初始姿势 |
| 绑定逆矩阵 | Bind Inverse | 把顶点倒回关节空间的逆变换 |
| 线性混合蒙皮 | LBS / SSD | 加权平均的基础蒙皮 |
| 双四元数蒙皮 | DQS | 保体积的 LBS 升级 |
| 姿势空间变形 | PSD | 用姿势插值修正形状(RBF) |
| 形变器 | Deformer | 直接改网格的工具 |
| 有限元法 | FEM | 切成小块解连续体 |
| 形变梯度 | Deformation Gradient F | 描述局部形变的核心量 |
| 应力/应变 | Stress / Strain | 受力/形变程度 |
| 超弹性 | Hyperelasticity | 用能量函数定义的大形变弹性 |
| 共旋 | Corotational | 先抽旋转再算形变 |
| 显式/隐式 | Explicit / Implicit | 时间积分两种策略 |
| 切线刚度 | Tangent Stiffness K | Newton 迭代用的"斜率" |
| 多重网格 | Multigrid | 加速解线性系统 |
| 径向基函数 | RBF | 距离加权的插值 |

---

## 8. 学习路径建议(按阶段)

> 不要一次全啃完。**先能动手,再补理论。**

**阶段 0(1–2 周):动手感受**
- 在 Blender/Maya 里给一个简单角色(或现成骨架)刷权重、摆姿势、看肘部塌陷 → 对 LBS 有体感。
- 关键词:`weight painting tutorial`、`armature deform`。

**阶段 1(2–4 周):绑定的"通用语言"**
- 学 3.1–3.3(向量、矩阵、四元数),配合练习:自己用代码拼一个关节层级、算 world 矩阵、做一次 LBS。
- 推荐:Freya Holmér 的《Math for Game Devs》视频系列;Games101/102 的骨骼动画章节。

**阶段 2(4–8 周):高级蒙皮**
- 读 PSD(Lewis 2000)原论文 → 实现一个最小 RBF 蒙皮修正;再学 DQS。
- 这时你已经能看懂[[FEM蒙皮 vs 关节蒙皮-迪士尼与战神]]的第三、五、六节(战神那半)了。

**阶段 3(8 周+):FEM(可选,进阶)**
- 补 3.4–3.5、4、5,然后读 McAdams 2011,尝试实现一个 2D 的弹簧/FEM 软组织。
- 推荐:Sifakis & Barbic 的 *FEM Simulation of 3D Deformable Solids*(SIGGRAPH Course),是最好的入门。

**贯穿**:随时回到[[FEM蒙皮 vs 关节蒙皮-迪士尼与战神]]对号入座——"哦,原来那句就是这里的 XX 概念"。

---

## 9. 推荐资源

- **绑定/蒙皮**
  - Rigging Dojo / 各种 Maya Rigging 教程(商业课)
  - Blender 官方骨骼绑定文档(免费入门)
- **数学**
  - Freya Holmér《Math for Game Devs》(YouTube,免费,极佳)
  - 3Blue1Brown《线性代数的本质》《微积分的本质》(建立直觉)
- **骨骼动画/蒙皮**
  - Games 101(闫令琪)骨骼动画章节
  - 论文:Lewis et al. 2000(Pose Space Deformation)
- **FEM 软体**
  - Sifakis & Barbic, *FEM Simulation of 3D Deformable Solids*(SIGGRAPH Course)
  - 论文:McAdams et al. 2011(见上篇参考文献)

---

## 附:一句话总结

> **绑定 = 搭骨架;蒙皮 = 骨架拽网格;LBS 是又快又烂的基线;所有高级技术(FEM、RBF、DQS)都在修 LBS 的"形变不自然"。数学上,向量/矩阵/四元数是地基,微积分+线性系统是 FEM 的引擎,RBF 是数据驱动蒙皮的钥匙。**
