#### 着色器
- **把计算尽量从片元着色器往顶点着色器挪。**

举个例子：如果你要将一个颜色变量乘以 2。

- 放在**片元着色器**里：屏幕如果是 1080p，全屏显示这个材质时，乘法运算要执行 **200多万次**。
    
- 放在**顶点着色器**里：假设模型只有 3000 个顶点，乘法运算只执行 **3000次**，算完后结果会自动平滑过渡给片元。
    

**总结：** 顶点管“结构”和“低频运算”，片元管“细节”和“高频运算”。