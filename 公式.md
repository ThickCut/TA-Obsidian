### 有机溶解
- **公式**：最终值 = 渐变值 - 噪声值 - 裁剪阈值
	-  `clip(gradient - noise - _Cutout)`
<br>
---
<br>
### 加法混合

> [!summary] 混合的核心数学公式
> 
> 屏幕上的每一次半透明颜色混合，GPU 都在执行以下线性向量加法公式：
> 
> $$C_{result} = (C_{src} \times F_{src}) + (C_{dst} \times F_{dst})$$
> 
> - $C_{result}$：最终输出到屏幕的颜色
>     
> - $C_{src}$：源颜色（当前 Shader 计算出的颜色）
>     
> - $C_{dst}$：目标颜色（屏幕上原本就已经存在的背景色）
>     
> - $F_{src}$：源混合因子
>     
> - $F_{dst}$：目标混合因子
>     

#### 1. 纯加法混合 Blend One One 


**数学实现：**

将 $F_{src}$ 和 $F_{dst}$ 都设为 1。完全无视贴图的 Alpha 通道。

$$C_{result} = C_{src} + C_{dst}$$

**视觉效果与应用场景：**

- **效果**：将两张图的颜色值直接暴力相加。因为只加不减，叠加得越多越亮（趋近于纯白）

- **场景**：爆炸的核心强光、刺眼的激光、纯黑底色的特效贴图（黑色 $RGB=0$ 加在背景上等于没加）


#### 2. 柔和加法混合 Blend SrcAlpha One


**数学实现：**

将 $F_{src}$ 设为当前输出颜色的 Alpha 值（$A_{src}$），$F_{dst}$ 设为 1。

$$C_{result} = (C_{src} \times A_{src}) + C_{dst}$$

**视觉效果与应用场景：**

- **效果**：不仅做加法，还利用贴图的 Alpha 通道来控制叠加强度。Alpha 越接近 0 的地方（如贴图边缘），融入背景越柔和。

- **场景**：带柔和边缘的光晕、魔法阵、发光粒子（Unity Particle System 加法材质的默认模式）。


> [!warning] 必背：半透明特效的“前置条件”
> 
> 在写任何加法混合 Shader 时，必须在 `Pass` 块或 `SubShader` 块中配置以下两个渲染状态，否则会出现严重的渲染层级和遮挡错误：
> 
> OpenGL Shading Language
> 
> ```
> Tags { "Queue"="Transparent" } // 1. 将渲染队列设为透明（在所有不透明物体之后渲染）
> ZWrite Off                   // 2. 关闭深度写入（防止透明物体互相用包围盒遮挡）
> ```

---
<br>
### Ndot

在图形渲染和 Shader 编写中，**NdotV** 是最基础、最核心的变量之一。它代表了表面**法线 (Normal)** 与 **视线方向 (View Direction)** 的点积 (Dot Product)。

简单来说，它衡量的是**你（摄像机）正以多大的角度观察这个物体表面**。

#### 核心定义

- **N (Normal Vector):** 物体表面某一点垂直于表面的方向向量。

- **V (View Direction):** 从物体表面该点指向摄像机的向量。

- **点积 ($\cdot$):** 在数学上，两个归一化向量的点积结果等于它们夹角的余弦值 ($\cos\theta$)。

$$\text{NdotV} = \text{dot}(N, V) = \cos(\theta)$$

#### 视觉上的直观理解

想象你正对着一个球体：

- **当你看向球心时：** 视线方向与表面法线平行（夹角为 0°），$\cos(0) = 1$，**NdotV = 1**。

- **当你看向球体边缘时：** 视线方向与表面法线垂直（夹角为 90°），$\cos(90°) = 0$，**NdotV = 0**

| **NdotV 值** | **含义**  | **视觉效果**        |
| ----------- | ------- | --------------- |
| **接近 1**    | 视线正对表面  | 看到的往往是物体中心区域    |
| **接近 0**    | 视线与表面平行 | 看到的往往是物体的轮廓（边缘） |

#### 为什么它在 Shader 中至关重要？

NdotV 是实现许多视觉特效的“万能钥匙”，因为它能非常轻松地根据**视点**来改变材质表现。

##### 1. 边缘光 (Rim Light / Fresnel Effect)

这是 NdotV 最经典的应用。我们想让物体的边缘发光，就像背后有光照过来一样。

**逻辑：** 边缘处的 NdotV 趋近于 0，所以我们使用 `1 - NdotV` 即可提取边缘。

High-level shader language

```
// HLSL 代码片段
float3 N = normalize(i.normal);
float3 V = normalize(_WorldSpaceCameraPos - i.worldPos);
float NdotV = saturate(dot(N, V)); // saturate 将值限制在 0-1

// 边缘越接近 0，结果越大，从而产生发光效果
float rim = 1.0 - NdotV;
rim = pow(rim, _RimPower); // 使用幂运算控制边缘光的厚度
```

##### 2. 边缘透明度 (Opacity Falloff)

在制作能量护盾、幽灵或某些特效时，你可能希望物体在中心是透明的，但轮廓清晰可见。

**逻辑：** 此时直接使用 `NdotV` 作为透明度通道的输入即可。

##### 3. 各向异性材质 (Anisotropy)

在处理丝绸、金属拉丝等材质时，光照不仅取决于光源，还取决于视线与微观纹理方向的关系，`NdotV` 是计算这些复杂 BRDF（双向反射分布函数）的基础项。

#### 编写 Shader 时的注意事项

在使用 `NdotV` 时，新手最容易忽略两点：

1. **必须归一化 (Normalize)：** 点积运算的前提是 $N$ 和 $V$ 必须是单位向量（长度为 1）。如果传入的是未归一化的向量，结果将完全错误。
    
    - _错误：_ `dot(N, V)`
        
    - _正确：_ `dot(normalize(N), normalize(V))`
        
2. **截断 (Saturate/Clamp)：** 如果视线夹角大于 90 度（比如你在物体的背面），点积会产生负数。在大多数视觉逻辑中，这通常不是我们想要的，所以务必使用 `saturate(dot(N, V))` 将结果强制限制在 $[0, 1]$ 范围内。
<br>

---

### Pow

凡是需要调整对比度、控制衰减速度、或者让某个效果更“聚焦”的地方，几乎都会用到它。

除了边缘光（Rim Light / Fresnel），以下是 `pow` 函数在图形学中最经典的几个应用场景：

#### 1. 镜面高光 (Specular Highlight)

这是 `pow` 函数在计算机图形学中最古老、也是最著名的应用——**Blinn-Phong 光照模型**。

当你用手电筒照在苹果上，苹果表面会有一个反光的白点。如果不使用 `pow`，这个白点会极其巨大，让整个苹果看起来像在发白光。我们通过 `pow` 函数，可以把大面积的漫反射光斑，“压缩”成一个极小、极亮的高光点。

![通过 pow 函数控制高光的聚焦程度，AI 生成](https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcSXlQQ9KNwUCrL1BphcVDiWbR9In5xxyyBuyWbWiaPSROdrH4XtHwoSLuG1&s=10)

通过 pow 函数控制高光的聚焦程度.

> **底层逻辑：**
> $\text{Specular} = (\max(0, N \cdot H))^{\text{Gloss}}$ 
> 这里的 `Gloss` (光泽度) 通常是一个很大的数字（比如 32、64、128）。数字越大，`pow` 挤压得越厉害，高光点就越小，材质看起来就越光滑（比如从粗糙的木头变成锃亮的金属）。

#### 2. 距离衰减 (Distance Falloff)

现实世界中，光线的衰减遵循“平方反比定律”（距离远一倍，亮度变暗四倍）。在计算点光源（Point Light）或手电筒（Spot Light）照射范围时，必定会用到 `pow`。

- **光源衰减：** `pow(distance, 2)`让光线在靠近光源的地方很亮，但随着距离增加，亮度迅速跌落

- **深度雾效 (Fog)：** 很多高级的指数雾 (Exponential Fog) 会使用公式 $e^{-(\text{distance} \times \text{density})^2}$，底层的运算逻辑也是求幂，用来让远处的山脉被雾气迅速吞没


#### 3. 伽马校正与颜色空间转换 (Gamma Correction)

这是渲染管线中最隐蔽但也最关键的 `pow` 应用。 我们的显示器为了适应人眼的视觉特性，显示颜色时并不是线性的（它会把暗部压得更暗）。为了在 Shader 中进行正确的物理光照计算，我们需要在“线性空间 (Linear)”和“sRGB 空间”之间反复横跳。

- **sRGB 转 线性 (Gamma 解码)：** `pow(color, 2.2)`
    
- **线性 转 sRGB (Gamma 编码)：** `pow(color, 1.0 / 2.2)`
    

如果你的游戏画面看起来总是灰蒙蒙的、或者高光曝光过度，大概率是因为缺少了这步求幂运算。


#### 4. 溶解与噪声的对比度控制 (Noise Contrast)

在做**角色死亡溶解 (Dissolve)** 或者**火焰、护盾上的扰动纹理**时，我们通常会采样一张黑白云朵状的噪声图 (Noise Texture)。

噪声图原本的黑白过渡是非常平缓的（满屏的灰色）。如果你想让黑的更黑、白的更白，把边界分得特别清晰，直接对采样的结果求幂即可：

High-level shader language

```
// noise 的值域是 0~1
float noise = tex2D(_NoiseTex, uv).r; 

// 通过 pow(x, 3)，把灰色压暗，只保留最白的部分作为“火苗”或“星星”
float sharpNoise = pow(noise, 3.0); 
```

**总结一下：** 你可以把 `pow` 想象成 Photoshop 里的“曲线 (Curves)”工具。只要你的数据范围在 0 到 1 之间，用 `pow` 就可以随心所欲地把线性的渐变，捏成你想要的各种陡峭弧度。

---
## 1. 矢量与基础运算

矢量运算是图形学的基础，主要用于计算角度、朝向和投影。

- **点乘 (Dot Product)**
    
    - **公式**：$A \cdot B = \vert{}A\vert{}\vert{}B\vert{}\cos(\theta)$
        
    - **物理意义**：计算两个向量的相似度或投影。如果两个向量都是单位向量，结果就是它们夹角的余弦值。
        
    - **应用**：计算光照迎光面（夹角越小越亮）、判断正反面（结果为正则朝向一致）。
        
    - **代码**：`dot(A, B)`
        
- **叉乘 (Cross Product)**
    
    - **公式**：$A \times B = \vert{}A\vert{}\vert{}B\vert{}\sin(\theta) \vec{n}$
        
    - **物理意义**：得到一个垂直于这两个向量所在平面的新向量。
        
    - **应用**：计算法线（已知平面两个切线向量时）、判断一点在直线左侧还是右侧（常用于卡通渲染中的描边轮廓提取）。
        
    - **代码**：`cross(A, B)`
        
---
## 2. 核心光照模型 (Lighting Models)

光照模型决定了物体在环境中的立体感和材质质感。

- **兰伯特漫反射 (Lambert Diffuse)**
    
    - **公式**：$I_{diff} = \max(0, N \cdot L)$
        
    - _变量释义_：$N$ 为表面法线，$L$ 为指向光源的向量。
        
    - **应用**：最基础的无光泽表面受光计算。$\max(0, x)$ 确保背光面不会出现负数的亮度（即黑于纯黑）。
        
- **半兰伯特漫反射 (Half-Lambert)**
    
    - **公式**：$I_{half} = (N \cdot L \times 0.5) + 0.5$
        
    - **应用**：Valve 在《半条命》中提出的改良版，用于防止背光面死黑，常用于卡通渲染 (Toon Shading) 中的平滑过渡。
        
- **Blinn-Phong 高光 (Specular)**
    
    - **公式**：$I_{spec} = \max(0, N \cdot H)^p$
        
    - _变量释义_：$p$ 为高光反光度（Glossiness），$H$ 是半角向量，即视线向量 $V$ 与光源向量 $L$ 的中间向量：
        
        $$H = \frac{L + V}{\vert{}L + V\vert{}}$$
        
    - **应用**：计算金属、塑料、水面等材质的高光反射。
        
- **菲涅尔效应 (Fresnel - Schlick 近似)**
    
    - **公式**：$F = F_0 + (1 - F_0)(1 - \max(0, V \cdot N))^5$
        
    - _变量释义_：$F_0$ 为基础反射率（通常非金属设为 $0.04$）。
        
    - **应用**：视线掠过物体边缘时反射率变高。常用于水面反光、汽车车漆、边缘发光（Rim Light）特效。
        
---
## 3. 形态与插值控制

这类函数用于控制颜色的混合、边缘的虚化或硬化。

- **线性插值 (Linear Interpolation)**
    
    - **公式**：$f(x, y, a) = x(1 - a) + y a$
        
    - **应用**：根据权重 $a$（范围从 $0$ 到 $1$）在两个颜色、纹理或位置之间混合。
        
    - **代码**：GLSL 用 `mix(x, y, a)`，HLSL 用 `lerp(x, y, a)`。
        
- **平滑阶跃 (Smoothstep)**
    
    - **核心多项式**：$S(x) = 3x^2 - 2x^3$ （当 $x$ 限制在 $[0,1]$ 之间时）
        
    - **应用**：创建一个具有平滑过渡的 S 型曲线。常用于消除锯齿、制作软阴影边缘、溶解特效 (Dissolve) 的边缘过渡。
        
    - **代码**：`smoothstep(edge0, edge1, x)`
        
---
## 4. UV 空间与矩阵变换

操作 UV 坐标可以实现纹理的流动、旋转和缩放。

- **2D 旋转矩阵 (2D Rotation Matrix)**
    
    - **公式**：
        
        $$\begin{bmatrix} u' \\ v' \end{bmatrix} = \begin{bmatrix} \cos(\theta) & -\sin(\theta) \\ \sin(\theta) & \cos(\theta) \end{bmatrix} \begin{bmatrix} u \\ v \end{bmatrix}$$
        
    - **应用**：雷达扫描效果、漩涡特效、粒子贴图的随机旋转。通常需要先将 UV 移动到中心 $[u-0.5, v-0.5]$，乘上矩阵后再移回 $+0.5$。
        
- **随时间流动 (Scrolling/Panning)**
    
    - **公式**：$UV_{new} = UV + \vec{v} \cdot t$
        
    - **应用**：水流、云层飘动、瀑布材质。其中 $t$ 通常取自系统时间 `_Time`。
        
---
## 5. SDF (符号距离场) 基础形状

在不需要贴图的情况下，直接用数学公式画图，常用于 UI 着色器或 Raymarching 屏幕特效。

- **画圆 (Circle)**
    
    - **公式**：$d = \vert{}P - C\vert{} - r$
        
    - _变量释义_：$P$ 为当前像素坐标，$C$ 为圆心坐标，$r$ 为半径。当 $d \le 0$ 时在圆内。
        
    - **应用**：雷达、波纹扩散、瞄准星。
        
- **画线段 (Line)**
    
    - **公式**：通过计算点到线段的垂直投影距离来求 $d$。结合 `smoothstep` 可以画出带有抗锯齿的线。