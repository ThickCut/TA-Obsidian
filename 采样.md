
---
#### 动态采样
<br>
一、涉及的公式

- 重映射：                         float offset = (noiseColor - 0.5) * 2.0;
$$\vec{V}_{dir} = (\text{Color} \times 2) - 1$$
- 动态采样：           float2 noiseUV = i.uv + _Time.y * _NoiseSpeed.xy;
$$UV_{final} = UV_{base} + \vec{V}_{dir} \times \text{Strength}$$
二、重难点

- 动静分离的 UV 管理
	- 绝对不能把扰动图的 UV 变化直接算到主 UV 上。扰动图自己用一套带时间的流动 UV，而主图初始状态必须是静止的，只接受算出来的结果
	- 特别注意：计算机图形学中，只要是纯黑白灰的图片，它的 R、G、B 三个通道的数值永远是完全相等的。
	- 不提取透明度a是因为在绝大多数普通的噪波贴图中，透明度通道（Alpha）要么根本不存在，要么全都是无效的“死数据”
```
// 1. 扰动图的 UV 在随时间流动
float2 noiseUV = i.uv + _Time.y * _NoiseSpeed.xy;
// 2. 提取出扰动数据 (0 ~ 1 之间)
half noiseColor = tex2D(_NoiseTex, noiseUV).r;
```

- 颜色到方向的重映射
	- 必须将其转换为 $[-1, 1]$ 区间，并乘以面板上暴露出的强度参数
```
// 3. 将 0~1 的单通道颜色转换为 -1~1 的偏移量
float offset = (noiseColor - 0.5) * 2.0; 

// (注：如果你用的是法线贴图作为扰动图，可以同时提取 r 和 g 两个通道)
// float2 offset = (noiseColor.rg - 0.5) * 2.0;
```

---